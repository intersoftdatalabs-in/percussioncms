<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/tx
           http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- Enable component scanning for test packages -->
    <context:component-scan base-package="com.percussion.delivery.feeds"/>

    <!-- Properties configuration -->
    <context:property-placeholder location="classpath:feed-validation.properties"/>

    <!-- Enable transaction management -->
    <tx:annotation-driven/>

    <!-- Test database configuration -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="org.h2.Driver"/>
        <property name="url" value="jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1"/>
        <property name="username" value="sa"/>
        <property name="password" value=""/>
    </bean>

    <!-- Hibernate configuration for tests -->
    <bean id="sessionFactory" class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="packagesToScan">
            <list>
                <value>com.percussion.delivery.feeds.data</value>
                <value>com.percussion.delivery.feeds.services.rdbms</value>
            </list>
        </property>
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">org.hibernate.dialect.H2Dialect</prop>
                <prop key="hibernate.hbm2ddl.auto">create-drop</prop>
                <prop key="hibernate.show_sql">true</prop>
                <prop key="hibernate.format_sql">true</prop>
            </props>
        </property>
    </bean>

    <!-- Transaction manager -->
    <bean id="transactionManager" class="org.springframework.orm.hibernate5.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <!-- Feed generator for tests -->
    <bean id="feedGenerator" class="com.percussion.delivery.feeds.PSFeedGenerator"/>

    <!-- Performance monitor -->
    <bean id="performanceMonitor" class="com.percussion.delivery.feeds.test.FeedPerformanceMonitor"
          factory-method="create">
        <constructor-arg value="${jmx.base-name}"/>
    </bean>

    <!-- XML Schema validator -->
    <bean id="schemaValidator" class="javax.xml.validation.SchemaFactory"
          factory-method="newInstance">
        <constructor-arg value="http://www.w3.org/2001/XMLSchema"/>
    </bean>

    <!-- Test execution listener -->
    <bean class="com.percussion.delivery.feeds.test.FeedTestExecutionListener"/>
</beans>
