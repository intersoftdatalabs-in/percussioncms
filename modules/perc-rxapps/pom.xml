<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.percussion</groupId>
        <artifactId>core</artifactId>
        <version>8.2.0-SNAPSHOT</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>
    <artifactId>perc-rxapps</artifactId>
    <version>8.2.0-SNAPSHOT</version>
    <name>perc-rxapps</name>
    <description>Assembles the Rhythmyx (Rx) application distribution that the CMS installer
        deploys into the running server. The module has no Java sources of its own; it uses the
        maven-antrun-plugin to drive the system/rxAppsCopy.xml Ant script that copies the bundled
        Rhythmyx application support, ObjectStore, and FastForward sample content out of the
        build tree into target/distribution/, and the maven-assembly-plugin to package that tree
        as the RxApp + RxFastForward tar.gz / zip artifacts that the installer ships.</description>
    <properties>
        <assembly-directory>${basedir}/target/distribution</assembly-directory>
        <jetty-directory>${basedir}/target/jetty</jetty-directory>
    </properties>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <id>populate perc distribution from jetty</id>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <phase>process-resources</phase>
                        <configuration>
                            <target>
                                <property name="BUILDVERSION" value="${project.parent.version}"/>
                                <property location="${assembly-directory}/RxApp" name="APPS_DIR"/>
                                <property location="${assembly-directory}/RxFastForward" name="FF_DIST_APPS_DIR"/>
                                <property location="true" name="WITHFF"/>
                                <ant antfile="rxAppsCopy.xml" dir="${basedir}/../../system" inheritRefs="true"/>
                            </target>
                        </configuration>
                    </execution>
                    <!-- <execution> <id>set jetty.sh</id> <phase>process-resources</phase>
						<goals> <goal>run</goal> </goals> <configuration> <target> <chmod dir="${assembly-directory}/bin"
						perm="755" includes="**/*.sh" /> </target> </configuration> </execution>
						<execution> <id>removeKeystore</id> <phase>process-resources</phase> <goals>
						<goal>run</goal> </goals> <configuration> <target> <delete file="${assembly-directory}/etc/keystore"
						/> </target> </configuration> </execution> -->
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <configuration>
                    <descriptors>
                        <descriptor>src/main/assembly/perc-assembly.xml</descriptor>
                    </descriptors>
                    <tarLongFileMode>posix</tarLongFileMode>
                    <appendAssemblyId>false</appendAssemblyId>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>single</goal>
                        </goals>
                        <phase>package</phase>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-site-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
