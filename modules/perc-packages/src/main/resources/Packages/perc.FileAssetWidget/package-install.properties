# Native deployer install for leftover binary TemplateDefs (#3674 / #2630).
# Dual-ship root *.templateDef generation is OFF; package build stages
# TemplateDef-N/ archive folders from pages/<id>/component-package.json.
page.installMode=native
