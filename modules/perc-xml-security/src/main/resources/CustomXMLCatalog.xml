<?xml version="1.0"?>
<!-- Operator-custom OASIS XML Catalog. No DOCTYPE (see PercussionXMLCatalog.xml). -->
<catalog xmlns="urn:oasis:names:tc:entity:xmlns:xml:catalog"
	prefer="system" xml:base="file://@@rxdir@@">
	<!-- Add any custom DTD, Entity, Namespace, XSL (import, include, document)
		URI's to this catalog file. The system will only process XML references that
		are defined in @@rxdir@@/PercussionXMLCatalog.xml or this file. This policy
		is to protect against XXE vulnerabilities. -->
</catalog>
