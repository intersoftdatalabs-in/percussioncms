# CMS 8.2 Smoke Test — Windows Install and Post-Install Server Startup Findings

**Generated:** 2026-07-22  
**Updated:** 2026-07-24 (address open PR review comments; redact install paths; clarify evidence sources)  
**Scope:** Catalog of warnings and errors observed during installation and post-installation server startup on Windows. This is a standalone summary; no external log or analysis file is required to interpret it.

**Evidence source for Issue 2:** Unless noted otherwise, Issue 2 findings come from **Jetty console stdout/stderr** of a foreground `StartJetty.bat` run — **not** from `server.log` (which was missing; see Issue 1). That is why console WARN/ERROR lines are catalogued here.

---

## Issue 1 — Post-Installation: Expected Log Files Are Missing 🛑

**Severity:** 🛑 CRITICAL — operators cannot perform post-install triage from the documented log files. The absence of `server.log` in particular means Issue 2.7 (Rhythmyx Spring init failure → HTTP 503) cannot be traced through the documented server log at all.

After the installer reported success on a fresh Windows install (`<InstallDir>`), the following expected log files were **not** present in their documented install locations:

| File | Expected role | Found |
|---|---|---|
| `server.log` | Server / application runtime log (used to triage post-install failures) | ❌ Not present |
| `installPackages.log` | Per-package install output (used to audit extension / package manager activity) | ❌ Not present |

Note: the `installPackages.log` absence is itself a finding independent of Issue 2.7 — even after the server start sequence began, no package-level audit trail is available for the post-install extensions.

**Impact:**
1. **Issue 2.7 is currently untraceable through `server.log`.** Without the server log, an operator reading only the on-disk artefacts cannot correlate the Spring init failure with stack frames, prior INFO/WARN context, or a thread name.
2. **`installPackages.log` absence** blocks any post-install extension audit — failed extension activation cannot be diagnosed from the filesystem.
3. Any failure that happens silently during install or post-install extension processing is invisible to the operator.

**Action:**
1. Confirm the exact expected paths for `server.log` and `installPackages.log` in the 8.2 install layout (`rxconfig/Logs/`, `logs/`, `jetty/base/logs/`, or under `rxconfig/Installer/install_logs/`).
2. If the installer is supposed to write these logs, file a defect — they are missing on this Windows install.
3. If they are written under a different path, document the actual location in the operator guide.
4. **Reproduce the install with logging re-enabled (verbose / debug) and capture the full `server.log`** before any further triage on Issue 2.7.

---

## Issue 2 — Jetty Starts With WARNINGS and ERRORS

**Severity:** 🛑 Multiple — see sub-issues.

**Source note:** All 2.x excerpts below are from **console** output of `StartJetty.bat` (foreground), because `server.log` was not present (Issue 1).

### 2.1 — Jetty forks a second JVM for `perc-logging` and `perc` 🟡

```
WARN : Forking second JVM due to forking module(s): [perc-logging, perc]. Use --dry-run to generate the command line to avoid forking.
```

**Impact:** A second JVM is forked at startup to host `perc-logging` and `perc`. Each fork doubles memory use and slows startup; it also breaks log line ordering between the parent and child JVMs.
**Action:**
1. Run the start command with `--dry-run` to inspect the underlying command line and identify the fork boundary.
2. Remove the forking requirement for `perc-logging` and `perc` so they run in-process in the typical CMS install.

### 2.2 — SLF4J multiple providers → NOP logger fallback 🟡

```
SLF4J(W): Class path contains multiple SLF4J providers.
SLF4J(W): Found provider [org.apache.logging.slf4j.SLF4JServiceProvider@…]
SLF4J(W): Found provider [org.eclipse.jetty.logging.JettyLoggingServiceProvider@…]
SLF4J(W): See https://www.slf4j.org/codes.html#multiple_bindings for an explanation.
SLF4J(I): Actual provider is of type [org.apache.logging.slf4j.SLF4JServiceProvider@…]
SLF4J(E): A service provider failed to instantiate:
org.slf4j.spi.SLF4JServiceProvider: org.apache.logging.slf4j.SLF4JServiceProvider not a subtype
SLF4J(E): A service provider failed to instantiate:
org.slf4j.spi.SLF4JServiceProvider: org.eclipse.jetty.logging.JettyLoggingServiceProvider not a subtype
SLF4J(W): No SLF4J providers were found.
SLF4J(W): Defaulting to no-operation (NOP) logger implementation
SLF4J(W): See https://www.slf4j.org/codes.html#noProviders for further details.
```

**Impact:** Application logging routed through SLF4J (including audit and security events) is silently dropped. Subsequent log lines from the application code are missing from the runtime log.
**Action:**
1. Pick a single SLF4J provider — recommend Log4j 2's `log4j-slf4j2-impl`, since it is already on the classpath.
2. Exclude Jetty's `jetty-slf4j-impl` (or vice versa) from the runtime classpath.
3. Verify by reproducing a known INFO-level event and confirming it appears in the log file.

### 2.3 — Deprecated Jetty `ShutdownMonitor` API in use 🟢

```
WARN [ShutdownMonitor] org.eclipse.jetty.server.ShutdownMonitor is deprecated,
      and has been replaced with org.eclipse.jetty.server.ShutdownService
```

**Impact:** Current Jetty 12 still tolerates this, but the API will be removed in a future Jetty release.
**Action:** Update the Jetty base configuration to use `ShutdownService` instead of `ShutdownMonitor`.

### 2.4 — Deprecated `CookieConfig.setComment` in `Rhythmyx.xml` 🟢

```
WARN [XmlConfiguration] Deprecated method public void
      org.eclipse.jetty.ee11.servlet.SessionHandler$CookieConfig.setComment(java.lang.String)
      in file:///<InstallDir>/jetty/base/webapps/Rhythmyx.xml
```

**Impact:** Same as 2.3 — current Jetty tolerates it; future Jetty will not.
**Action:** Update `Rhythmyx.xml` to use the current `CookieConfig` API (drop or replace the `setComment` call).

### 2.5 — Empty arguments in `jetty/upstream/etc/jetty.xml` 🟢

```
WARN [XmlConfiguration] Ignored arg [[]] in file:///<InstallDir>/jetty/upstream/etc/jetty.xml
```

(repeated three times in the **console** startup output)

**Impact:** Informational — Jetty logs the warning but otherwise starts correctly. **Not a functional problem** for this smoke run.
**Action:** Remove the empty `[]` arg from the Jetty XML configuration or supply a non-empty default (hygiene; low priority).

### 2.6 — Missing XML schemas for Jetty `DigesterFactory` 🟢

```
WARN [DigesterFactory] The XML schema [XMLSchema.dtd] could not be found. This is very likely to break XML validation if XML validation is enabled.
WARN [DigesterFactory] The XML schema [datatypes.dtd] could not be found.
WARN [DigesterFactory] The XML schema [xml.xsd] could not be found.
```

**Impact:** Jetty falls back to non-validating XML parsing. CMS-side XML validation is not currently enabled, so **no functional break** observed.
**Action:** Either supply the DTD/XSD files on the classpath or document the intentional non-validating mode. No urgent fix.

### 2.7 — Rhythmyx webapp fails Spring init: HTTP 503 Service Unavailable 🛑

```
WARN [WebAppContext] Failed startup of context oeje11w.WebAppContext@…{ROOT,/,b=[file:///<InstallDir>/jetty/base/webapps/Rhythmyx/, …]}{<InstallDir>\jetty\base\webapps\Rhythmyx}
org.springframework.beans.factory.UnsatisfiedDependencyException:
  Error creating bean with name 'relationshipSummaryService' defined in URL
    [jar:file:/…/sitemanage-8.2.0-SNAPSHOT.jar
     !/com/percussion/share/relationship/service/impl/PSRelationshipSummaryService.class]:
  Unsatisfied dependency expressed through constructor parameter 2:
    No qualifying bean of type 'com.percussion.share.dao.IPSRelationshipCataloger' available:
      expected single matching bean but found 2: contentItemDao, relationshipCataloger
Caused by: org.springframework.beans.factory.NoUniqueBeanDefinitionException:
  No qualifying bean of type 'com.percussion.share.dao.IPSRelationshipCataloger' available:
    expected single matching bean but found 2: contentItemDao, relationshipCataloger
```

Symptom observed from a browser probe:

```
HTTP ERROR 503 Service Unavailable
URI:    http://localhost:9992/
STATUS: 503
MESSAGE: Service Unavailable
```

**Impact:** 🛑 Blocking — Jetty binds on port `9992`, but every HTTP request to the Rhythmyx ROOT webapp returns **503 Service Unavailable** because the Spring `WebApplicationContext` for `ROOT` never finished initializing. The CM1 UI is unreachable. This is why login/logout checklist items on issue #1369 remain blocked.

**Cause:** `PSRelationshipSummaryService` (in `sitemanage`) declares a constructor parameter of type `IPSRelationshipCataloger`, and Spring finds **two** beans matching that type — `contentItemDao` and `relationshipCataloger` — instead of exactly one.

**Action:**
1. Add `@Primary` to the intended `relationshipCataloger` bean (or `@Qualifier` on the constructor parameter), OR remove/rename the duplicate `contentItemDao` bean so only one bean matches `IPSRelationshipCataloger`.
2. Inspect `sitemanage` for any recent change that introduced a second bean of that type (duplicate `@Component` / `@Service` / `@Repository` scan, or a stray `<bean>` declaration in a Spring config XML).
3. Re-deploy and confirm the Rhythmyx webapp starts cleanly; the HTTP probe should then return 200.

---

## Issue 3 — Windows-Native Issues Detected 🟡

**Severity:** 🟡 Evidence-bounded — no Windows-native issues were **observed in the captured console startup content**, but some categories cannot be fully closed from the available evidence alone because Issue 1 (missing install logs) and the foreground-only run (no Windows service install) remove sources that would otherwise surface those classes of issue.

The reviewer's checklist question — "Were there any Windows native issues detected?" — is answered below with explicit evidence-bounded language.

| Windows-native category | Problem observed? | Notes |
|---|---|---|
| Path / filesystem layout (backslashes, drive letters, long paths > 260 chars, UNC paths) | **No problem observed** in the captured console output. Paths that appeared used well-formed Windows layouts (drive letter + separators). Long-path (>260 char) and UNC paths were **not exercised**, so those sub-cases remain untested — not a failure, only untested coverage. | Partial coverage |
| Permissions (ACL, UAC, file-system reparse points, symlinks) | **None visible** in the console startup. Package-manager permission failures would typically land in install-time logs (`installPackages.log`), which were missing (Issue 1), so this category is **not fully falsifiable** from captured content alone. | Cannot fully falsify |
| Native library / DLL load failures (`UnsatisfiedLinkError`, wrong-arch DLL) | **No problem observed** — a JVM-side native failure would surface in the Jetty start sequence and was not observed. | Evidence-bearing |
| Code-page / encoding errors (`Cannot encode character`, `Cp1252`) | **No problem observed** — log lines rendered correctly across the sample. | Evidence-bearing |
| Service / driver issues (Windows Service Control Manager, Procrun) | **Not exercised** — this run was a foreground start (`StartJetty.bat`), not a service install (`install-jetty-service.bat` → Procrun). | Out of run scope |
| Windows Registry | **Not applicable to this product surface.** Percussion CMS / Jetty is a Java application; normal install and server start do **not** write product state to the Windows Registry. Registry was not an expected failure channel for this smoke run and is not listed as a residual risk. | N/A (Java product) |

### Methodology note

This document is bounded by the source content available at the time of capture:

1. **Captured**: Jetty **console** stdout/stderr from the foreground `StartJetty.bat` run on Windows, plus the HTTP 503 probe.
2. **Not captured**: `server.log` (Issue 1 — absent), `installPackages.log` (Issue 1 — absent), `installer.log` (not provided), the Windows event log, Procrun service-wrapper logs (no service install was performed).
3. As a result, **no claim in this document should be read as a full Windows-native-issues audit**. Evidence-bearing rows can be defended from the captured console content. Gaps to close on a follow-up smoke run: (a) reproduce install with `installPackages.log` capture enabled, (b) service install via `install-jetty-service.bat`, (c) collect a fresh `server.log` after the server reaches a steady state.

### Cross-platform comparison

All observed issues catalogued as 2.1–2.7 are **JVM / classpath / Spring / Jetty / SLF4J** issues that would manifest identically on Linux or macOS; none are demonstrably Windows-specific based on the captured content. A dedicated Windows-only follow-up (long-path layouts, Procrun service install) remains useful for coverage, not because this run found Windows-native defects.

---

## Cross-Cutting Summary

| # | Issue | Severity | Backlog |
|---|-------|----------|---------|
| 1 | Post-install: `server.log` and `installPackages.log` not in place | 🛑 | #1483 |
| 2.1 | Jetty forks second JVM for `perc-logging`, `perc` | 🟡 | #1485 |
| 2.2 | SLF4J multiple providers → NOP logger | 🟡 | #1484 |
| 2.3 | Deprecated `ShutdownMonitor` API in use | 🟢 | #1486 |
| 2.4 | Deprecated `CookieConfig.setComment` in `Rhythmyx.xml` | 🟢 | #1486 |
| 2.5 | Empty args in `jetty/upstream/etc/jetty.xml` (×3) | 🟢 | #1487 |
| 2.6 | Missing XML schemas for `DigesterFactory` | 🟢 | #1487 |
| 2.7 | Rhythmyx webapp fails Spring init (HTTP 503) | 🛑 | #1482 (**blocks UI**) |
| 3 | Windows-native issues (evidence-bounded) | 🟡 | No product defect; optional QA follow-up only |

### Recommended Next Steps (priority order)

1. **Issue 2.7 (Rhythmyx Spring init)** — fix duplicate `IPSRelationshipCataloger` beans; verify HTTP 200. **Blocks login/logout on #1369.**
2. **Issue 1 (missing log files)** — confirm expected paths; reproduce install with verbose logging; capture `server.log` / `installPackages.log`.
3. **Issue 2.2 (SLF4J provider conflict)** — single provider so application logs stop dropping to NOP.
4. **Issue 2.1 (forking JVMs)** — re-run start with `--dry-run`; rework `perc-logging` / `perc` to run in-process.
5. **Issues 2.3 / 2.4 (deprecated Jetty APIs)** — replace with current APIs.
6. **Issues 2.5 / 2.6** — config hygiene / document non-validating mode.
7. **Optional QA follow-up** — Windows service install (`install-jetty-service.bat`) + long-path layouts (not required to close #1369 once findings are filed).
