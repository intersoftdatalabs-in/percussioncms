**Mitigation (commit `6288f6b6ee`):** Replaced the contradictory mirror-copy sentence in §3a with two explicit, numbered branches: (a) `Widgets/` ⇄ `Resources/` lockstep confirmed → update **both** for visual parity; (b) not in lockstep → update **only `Widgets/`** (source of truth), leave `Resources/` untouched, and explicitly forbid editing `Resources/` from this skill. The original "otherwise the `Resources/` copy is canonical for the legacy UI" was leaving the `Widgets/` copy treatment ambiguous; that ambiguity is resolved and the negative-case behavior is now enforceable. Erlang re-review (`## Re-review`, recommendation **approve**) appended in `docs/ai-generated/code-reviews/feat-skill-add-locale-support-erlang.md`.
