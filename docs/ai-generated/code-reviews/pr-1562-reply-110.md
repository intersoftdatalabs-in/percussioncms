**Mitigation (commit `6288f6b6ee`):** Tightened the calendar picker pre-condition in `modules/ai-shared-develop/src/main/resources/skills/add-locale-support/SKILL.md` §3a from "your code needs a visible widget entry" to "only if the new locale is itself a calendar region, **or** your UI team has confirmed that a picker entry is required for this locale". The "code needs" branch is removed entirely so an implementer cannot over-extend; the new wording is gated on either an objective property (locale is a calendar region) or an explicit UI-team confirmation. Erlang re-review (`## Re-review`, recommendation **approve**) appended in `docs/ai-generated/code-reviews/feat-skill-add-locale-support-erlang.md`.
