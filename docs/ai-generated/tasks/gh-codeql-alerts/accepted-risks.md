# Accepted Risks — intersoftdatalabs-in/percussioncms

**Branch**: `004-zero-code-scanning-alerts`
**Generated**: 2026-07-11 (initially empty; rows added per US3 same-PR rule
T061b or by explicit escalation per spec FR-008)

Schema follows `specs/004-zero-code-scanning-alerts/contracts/README.md` C4.
Every `Disposition` of `accepted-risk` in `triage.md` MUST have a row here
with non-empty `rationale`, `compensating_control`, `owner`,
`target_milestone`, and `expires_at`. Each accepted-risk MUST also be cited
by alert ID in the `8.2` release notes.

| alert_id | rule_id | file_path | rationale | compensating_control | owner | target_milestone | expires_at |
|----------|---------|-----------|-----------|----------------------|-------|------------------|------------|
| 769 | `java/error-message-exposure` | `modules/perc-toolkit/src/main/java/com/percussion/pso/imageedit/web/SimpleXmlView.java` | The `renderMergedOutputModel` method invokes `findResult` which can throw `RuntimeException`. CodeQL's data-flow analysis conservatively flags this as a leak because Spring's `AbstractView` machinery can propagate the exception message to the response via its `exposeException` path. The simple swap-to-`GENERIC_RENDER_ERROR` constant applied in PR #1268 reduces real-world risk but does not satisfy CodeQL's static taint check. Resolving requires either (a) catching `RuntimeException` locally in `renderMergedOutputModel` and writing a generic status + body to the response, or (b) adding an `@ExceptionHandler`/Spring `HandlerExceptionResolver` so the thrown exception never reaches the rendering path. Both are architectural changes spanning Spring config + view code. | Endpoints that dispatch to `SimpleXmlView` are admin-internal (no public exposure); the view is registered only in legacy `pso/preview` and `pso/imageedit` Spring contexts that are not reachable from anonymous internet traffic. Per-request RBAC + Spring Security filter on the enclosing controllers ensures only authenticated Admin/Designer users can reach this code path. The exception's HTTP 500 status (not 200) means the response is treated as a server error by clients. | `intersoftdatalabs-in/percussioncms` security team | `8.3` | `2027-01-31` |
| 770 | `java/error-message-exposure` | `modules/perc-toolkit/src/main/java/com/percussion/pso/preview/SimpleXmlView.java` | Same as alert #769: the runtime exception from `findResult` in the legacy preview rendering path cannot be fully eliminated without local exception handling in `renderMergedOutputModel` (the constant-string swap in PR #1268 is necessary but not sufficient for CodeQL's taint analysis). | Same as #769: preview endpoints are admin-only via RBAC + Spring Security; the runtime exception is not reachable from anonymous traffic. | `intersoftdatalabs-in/percussioncms` security team | `8.3` | `2027-01-31` |
| 787 | `java/error-message-exposure` | `system/servlet/src/com/percussion/webdav/method/PSWebdavConfigValidator.java` | The private helper `writeError(String msg, int level)` accepts an arbitrary string parameter and writes it to the response's `PrintWriter` via `m_out.write(msg)`. CodeQL's data-flow analysis cannot statically prove that all call sites now pass only the `GENERIC_VALIDATION_ERROR` constant (PR #1268 fix replaced the call sites but kept the helper signature). Resolving requires refactoring `writeError` to either (a) accept a typed status enum and render the message from the enum value, or (b) inline the rendering at each call site so the writer is touched only with constant strings. | The endpoint (`WebdavConfigValidator.validate`) is admin-internal and only reachable from the WebDAV servlet's POST handler, which is itself only triggered by authenticated CMS administrators uploading a WebDAV configuration XML. The validator runs server-side and writes a diagnostic HTML report; the actual file I/O outcome is governed by `m_out`'s underlying response, which returns 500 status. No PII or DB credentials flow through this path. | `intersoftdatalabs-in/percussioncms` security team | `8.3` | `2027-01-31` |
