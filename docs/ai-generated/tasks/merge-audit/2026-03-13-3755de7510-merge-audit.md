# Merge audit — `3755de7510` (2026-03-13)

**Commit:** `3755de7510` — *Merge branch 'development' of github.com-intersoft:intersoftdatalabs-in/percussioncms into development*  
**Author:** Nate Chadwick  
**Date:** 2026-03-13 09:17:43 -0400

## What kind of merge this was

This was **not** a clean FF of one line of history. It merged two **divergent** tips of `development`:

|          Parent          |     SHA      |                                                                                   Character                                                                                    |
|--------------------------|--------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **P1 (local / ours)**    | `d93fb2f965` | Contained Jetty **12.1** + **Artemis** cutover (`88ca5716b0`). Did **not** yet contain commons-httpclient removal.                                                             |
| **P2 (remote / theirs)** | `f96a7f0890` | Contained Jetty **12.0.25** + **ActiveMQ Classic**, install/EHCache/ai-module work. Contained commons-httpclient removal (`6e78`/`8ecb`). **Did not** contain Artemis cutover. |

Parents differed by **~820 files**. Root `pom.xml` and MQ wiring were on **opposite** modernization paths.

## How conflict resolution went wrong

|          Artifact           |    P1 (local, preferred for many paths)     |             P2 (remote)             |                  Merge result `3755de7510`                   |               Effect                |
|-----------------------------|---------------------------------------------|-------------------------------------|--------------------------------------------------------------|-------------------------------------|
| Root `pom.xml` MQ           | Artemis 2.42 BOM, Jetty 12.1.7              | Classic ActiveMQ 6.2, Jetty 12.0.25 | **Kept P1 (Artemis/Jetty 12.1)**                             | Good for platform                   |
| `WebUI/pom.xml` MQ          | `artemis-server` + `artemis-jakarta-client` | `activemq-broker/client/spring/...` | **Manual hybrid: `activemq-spring` + `activemq-core:5.7.0`** | **Regression** — Dependabot #79/#80 |
| `WebUI/pom.xml` httpclient  | Still present                               | **Removed**                         | **Still present**                                            | **Regression** — Dependabot #81     |
| `jetty-ee11-env.xml`        | Artemis embedded                            | **Absent** (file only on P1 line)   | Kept Artemis (from P1)                                       | Good                                |
| `jetty-env.xml`             | **Absent** (deleted with Artemis work)      | Classic EE10 wiring                 | **Re-added from P2**                                         | Classic leftover                    |
| `etc/activemq/activemq.xml` | **Absent**                                  | Present                             | **Re-added from P2**                                         | Classic leftover                    |
| `perc-mq.xml`               | Artemis                                     | Classic                             | **Kept P1 Artemis**                                          | Good                                |

So the durable damage was concentrated in **WebUI dependency coordinates** and **re-introduction of Classic config files**, not a full platform rollback (root BOM stayed Artemis).

`WebUI/pom.xml` matched **neither** parent fully — classic conflict-resolution mash-up (same class of error as re-adding commons-httpclient).

## Confirmed product regressions (addressed or in flight)

|                           Issue                            |       Alert        |          Fix          |
|------------------------------------------------------------|--------------------|-----------------------|
| `commons-httpclient:3.1` reintroduced                      | Dependabot #81     | PR #1327              |
| `activemq-core`/`activemq-spring` 5.7.0 instead of Artemis | Dependabot #79/#80 | PR #1328              |
| Classic `jetty-env.xml` + `activemq.xml` re-added          | (config debt)      | PR #1328 deletes both |

## Other findings from automated file-set scan

### Remote-deleted tests still present after merge

P2 deleted (vs merge-base) nine feeds test files that remained in the merge result because P1 still had them:

- `deliverytiersuite/.../feeds/src/test/java/com/percussion/delivery/feeds/PSFeedGeneratorTest.java`
- `.../PSFeedServicePerformanceTest.java`
- `.../services/PSFeedServiceTestsBase.java`
- `.../services/PSFeedsServiceTests.java`
- `.../services/rdbms/PSFeedDaoTest.java`
- `.../junitrunners/{Concurrent,ConcurrentJunitRunner,Server,Threshold}.java`

**Interpretation:** not necessarily a security bug. Remote tip may have moved/renamed tests as part of Jetty12/startup work, or deleted flaky suites. **Action:** spot-check whether current `development` still intends these tests (they appear present) and whether P2 deletion was intentional cleanup. Low priority unless CI shows duplicates or failures.

### Merge vs P1 also pulled large sample war assets

`git show --stat 3755de7510` (first-parent view) reintroduced/packaged **CI_Home.war / EI_Home.war** resource trees under `modules/perc-jetty/.../webapps/`. Worth a human glance for accidental large binary/content commits, separate from MQ.

### Root platform versions

Because root `pom.xml` followed **P1**, post-merge tree kept Artemis/Jetty 12.1 direction. That limited blast radius: runtime configs that followed P1 stayed Artemis even while WebUI POM declared Classic jars (inconsistent classpath / Dependabot noise).

## Likely root cause (process)

1. Long-lived **diverged local `development`** vs **origin/development** (Artemis line vs pre-Artemis install line).
2. `git merge` with **large** WebUI/`pom` conflicts.
3. Manual resolution that mixed **coordinates from the older Classic line** into WebUI while keeping some Artemis env files.
4. No post-merge checklist verifying:
   - `WebUI/pom.xml` MQ artifacts match `jetty-ee11-env.xml` / root BOM
   - Dependabot / `mvn dependency:tree` for banned Classic coords
   - Intentionally deleted config files stayed deleted

## Recommended follow-ups

1. Merge PR **#1327** (httpclient) and **#1328** (Artemis restore) if not already.
2. Optional: script or CI check that fails if `activemq-core`, `activemq-spring`, or `commons-httpclient` appear as direct WebUI deps.
3. Optional: audit CI_Home/EI_Home war content introduced in this merge for bloat.
4. Avoid merging divergent `development` tips without reviewing `WebUI/pom.xml` and Jetty MQ files as a **coupled set**.

## Commands used

```bash
git show -s --format=fuller 3755de7510
git rev-parse 3755de7510^1 3755de7510^2
git merge-base --is-ancestor 88ca5716b0 3755de7510^1
git merge-base --is-ancestor 88ca5716b0 3755de7510^2
# content classification of key paths vs parents
```

